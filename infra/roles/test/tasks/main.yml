---
- name: Install Node.js dependencies
  ansible.builtin.command: npm ci
  args:
    chdir: "{{ checkout_dir }}"

- name: Run unit tests
  ansible.builtin.command: npm test
  args:
    chdir: "{{ checkout_dir }}"
